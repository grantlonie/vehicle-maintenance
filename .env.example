APP_TOKEN=change-me
PORT=3002
# Leave empty for ./data at the repo root. Use an absolute path in Docker (/data).
DATA_ROOT=
# Leave empty for local SQLite under DATA_ROOT/app.db
DATABASE_URL=
FRONTEND_DIST=
DEFAULT_DISPLAY_UNIT=km
